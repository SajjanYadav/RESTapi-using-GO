package main

//here we need to put the details of the database we want to connect to it and the user with
//which we want to connect and password for it 
const DBName = "inventory"
const DbUser = "root"
const DbPassword = ""